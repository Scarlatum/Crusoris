import { Entity } from "../entity";
export declare class Dot extends Entity {
    static DEFAULT_SIZE: number;
    static className: string;
    constructor(instance: any);
    activeAnimation(status: any): void;
}
